rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for Telegram mini app shop collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
